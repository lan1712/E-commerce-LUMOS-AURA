/**
 * Authentication and Authorization module.
 * Handles user registration, login, JWT token management, and security configuration.
 */
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {}
)
package com.example.be.auth;
