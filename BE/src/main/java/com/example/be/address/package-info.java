/**
 * Address Management module.
 * Handles user shipping address CRUD operations and default address management.
 */
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {}
)
package com.example.be.address;
